$(function () {
    $(".fancybox").fancybox({
        openEffect: "none",
        closeEffect: "none"
    });
})

$(function () {
    $('.datetimepicker').datetimepicker({
        // format: 'H:mm',
        locale: 'ru',
        format: 'DD/MM/YYYY HH:mm',
    });
});
